import { typedStyles } from './styleHelpers'

const commonStyles = typedStyles({
  flexRowWrap: {
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
})

export default commonStyles
